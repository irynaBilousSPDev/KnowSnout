# PDF visual references (Organic kit)

Generated locally via PyMuPDF from `docs/design/*.pdf`.  
Not committed (large PNGs). Regenerate:

```bash
python scripts/extract_pdf_refs.py
```

Active visual truth until Variant 12 color pass: Organic cream + sage CTA + Caprasimo/Figtree.
